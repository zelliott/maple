method 1:

1. zip contents of web_app folder (not folder itself)
2. go to "Upload and Deploy" on Custom-env-1
3. Increment name "web_app_x" and upload

method 2:

1. zip contents of web_app folder (not folder itself)
2. upload to s3
3. use launch_url